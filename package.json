{
  "name": "mediaplayer",
  "version": "1.0.0",
  "description": "building media player video",
  "scripts": {
    "start": "live-server",
    "build": "tsc src/**/*.ts src/plugins/*.ts src/plugins/**/*.ts --outDir lib",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Juan Gaybre",
  "license": "MIT",
  "devDependencies": {
    "live-server": "^1.2.1"
  }
}
